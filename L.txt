<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Сайт для Донатов</title>
    <!-- Подключение Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Пользовательские стили для шрифта Inter */
        body {
            font-family: 'Inter', sans-serif;
        }
        /* Стили для модального окна */
        .modal-overlay {
            background-color: rgba(0, 0, 0, 0.7); /* Затемнение фона */
            z-index: 999; /* Поверх всего */
        }
        .modal-content {
            z-index: 1000; /* Поверх оверлея */
        }
        /* Стили для индикатора загрузки */
        .loader {
            border: 4px solid #f3f3f3; /* Light grey */
            border-top: 4px solid #3498db; /* Blue */
            border-radius: 50%;
            width: 24px;
            height: 24px;
            animation: spin 2s linear infinite;
            display: inline-block;
            vertical-align: middle;
            margin-left: 8px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="bg-gradient-to-r from-purple-800 via-indigo-900 to-blue-900 min-h-screen flex flex-col items-center justify-center p-4">

    <!-- Главный контейнер сайта -->
    <div class="bg-gray-800 p-8 rounded-2xl shadow-2xl text-center max-w-3xl w-full my-8">
        <!-- Блок статуса сервера Minecraft -->
        <div class="bg-gray-700 p-4 rounded-xl mb-6 text-white text-lg font-semibold flex flex-col sm:flex-row justify-center items-center space-y-2 sm:space-y-0 sm:space-x-4">
            <span>Статус Minecraft сервера: <span id="serverStatusText" class="text-green-400">Загрузка...</span></span>
            <span id="playerCount" class="text-gray-400 text-sm"></span>
            <span class="text-gray-400 text-sm">(donator34.gamely.pro:20146)</span>
            <span id="lastUpdatedTime" class="text-gray-400 text-sm"></span>
        </div>

        <h1 class="text-5xl font-extrabold text-white mb-6">Добро пожаловать на Наш Сайт Донатов!</h1>
        <p class="text-gray-300 text-lg mb-8">Поддержите нас и получите эксклюзивные привилегии!</p>

        <!-- Секция с описанием донатов -->
        <h2 class="text-4xl font-bold text-white mb-6 mt-12">Наши Уровни Доната</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Карточка Донат VIP -->
            <div class="bg-gray-700 p-6 rounded-xl shadow-lg border border-purple-700 transform transition duration-300 hover:scale-105">
                <h3 class="text-3xl font-semibold text-purple-400 mb-3">VIP</h3>
                <p class="text-gray-300 mb-4">Эксклюзивные привилегии и бонусы для самых преданных!</p>
                <p class="text-white text-2xl font-bold mb-4">92 рубля</p>
                <button class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-purple-500 focus:ring-opacity-50 w-full open-donation-modal-btn" data-tier="vip">
                    Выбрать VIP
                </button>
            </div>

            <!-- Карточка Донат Premium -->
            <div class="bg-gray-700 p-6 rounded-xl shadow-lg border border-yellow-600 transform transition duration-300 hover:scale-105">
                <h3 class="text-3xl font-semibold text-yellow-400 mb-3">Premium</h3>
                <p class="text-gray-300 mb-4">Расширенный доступ и специальные возможности.</p>
                <p class="text-white text-2xl font-bold mb-4">50 рублей</p>
                <button class="bg-yellow-600 hover:bg-yellow-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-yellow-500 focus:ring-opacity-50 w-full open-donation-modal-btn" data-tier="premium">
                    Выбрать Premium
                </button>
            </div>

            <!-- Карточка Донат Standard -->
            <div class="bg-gray-700 p-6 rounded-xl shadow-lg border border-green-600 transform transition duration-300 hover:scale-105">
                <h3 class="text-3xl font-semibold text-green-400 mb-3">Standard</h3>
                <p class="text-gray-300 mb-4">Базовая поддержка и благодарность.</p>
                <p class="text-white text-2xl font-bold mb-4">20 рублей</p>
                <button class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-green-500 focus:ring-opacity-50 w-full open-donation-modal-btn" data-tier="standard">
                    Выбрать Standard
                </button>
            </div>
        </div>

        <!-- Сообщение о подтверждении (скрыто по умолчанию) -->
        <div id="confirmationMessage" class="hidden mt-8 p-6 bg-green-700 text-white rounded-xl shadow-lg text-lg">
            Спасибо за вашу поддержку! Ваша транзакция успешно обработана.
        </div>
    </div>

    <!-- Модальное окно меню донатов (скрыто по умолчанию) -->
    <div id="donationMenu" class="fixed inset-0 hidden modal-overlay flex items-center justify-center p-4">
        <div class="modal-content bg-gray-900 p-8 rounded-2xl shadow-2xl text-center max-w-lg w-full transform transition-all duration-300 ease-out scale-95 opacity-0">
            <h2 class="text-3xl font-bold text-white mb-6" id="donationModalTitle">Выберите Донат</h2>

            <!-- Опция доната (динамически заполняется) -->
            <div class="bg-gray-800 p-6 rounded-xl shadow-lg mb-6 border border-purple-700">
                <h3 class="text-2xl font-semibold text-purple-400 mb-3" id="modalDonationTierName">Донат VIP</h3>
                <p class="text-gray-300 mb-4" id="modalDonationTierDescription">Получите эксклюзивные привилегии и бонусы!</p>

                <div class="flex flex-col sm:flex-row justify-center items-center space-y-4 sm:space-y-0 sm:space-x-4 mb-4">
                    <button id="payDonationBtn" class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-purple-500 focus:ring-opacity-50 w-full sm:w-auto">
                        Оплатить <span id="modalDonationPrice">92</span> рубля
                    </button>
                    <button id="learnMoreVip" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-50 w-full sm:w-auto">
                        ✨ Узнать больше о VIP
                        <span id="vipLoader" class="loader hidden"></span>
                    </button>
                </div>
                <!-- Новая кнопка для генерации информации о влиянии доната -->
                <button id="generateDonationImpactBtn" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-full shadow-md transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-indigo-500 focus:ring-opacity-50 w-full sm:w-auto mt-4">
                    ✨ Узнать, на что пойдет донат
                    <span id="impactLoader" class="loader hidden"></span>
                </button>

                <!-- Место для отображения сгенерированного описания VIP -->
                <div id="vipDescription" class="mt-4 text-left text-gray-200 text-sm italic">
                    <!-- Здесь появится сгенерированное описание VIP -->
                </div>
                <!-- Место для отображения сгенерированного описания влияния доната -->
                <div id="donationImpactDescription" class="mt-4 text-left text-gray-200 text-sm italic">
                    <!-- Здесь появится сгенерированное описание влияния доната -->
                </div>
            </div>

            <!-- Кнопка закрытия меню -->
            <button id="closeDonationMenu" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-6 rounded-full shadow-md transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-red-500 focus:ring-opacity-50">
                Закрыть
            </button>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const donationMenu = document.getElementById('donationMenu');
            const donationMenuContent = donationMenu.querySelector('.modal-content');
            const closeDonationMenuBtn = document.getElementById('closeDonationMenu');
            const payDonationBtn = document.getElementById('payDonationBtn');
            const learnMoreVipBtn = document.getElementById('learnMoreVip');
            const generateDonationImpactBtn = document.getElementById('generateDonationImpactBtn');
            const vipDescriptionDiv = document.getElementById('vipDescription');
            const donationImpactDescriptionDiv = document.getElementById('donationImpactDescription');
            const vipLoader = document.getElementById('vipLoader');
            const impactLoader = document.getElementById('impactLoader');
            const modalDonationTierName = document.getElementById('modalDonationTierName');
            const modalDonationTierDescription = document.getElementById('modalDonationTierDescription');
            const modalDonationPrice = document.getElementById('modalDonationPrice');
            const confirmationMessage = document.getElementById('confirmationMessage');

            // Элементы для статуса сервера
            const serverStatusText = document.getElementById('serverStatusText');
            const playerCount = document.getElementById('playerCount'); // Новый элемент для количества игроков
            const lastUpdatedTime = document.getElementById('lastUpdatedTime');

            // --- Данные о донатах ---
            const donationTiers = {
                vip: {
                    name: 'Донат VIP',
                    description: 'Получите эксклюзивные привилегии и бонусы!',
                    price: 92
                },
                premium: {
                    name: 'Донат Premium',
                    description: 'Расширенный доступ и специальные возможности.',
                    price: 50
                },
                standard: {
                    name: 'Донат Standard',
                    description: 'Базовая поддержка и благодарность.',
                    price: 20
                }
            };

            // --- Функции для модальных окон ---

            function openModal(modalElement, contentElement) {
                modalElement.classList.remove('hidden');
                setTimeout(() => {
                    contentElement.classList.remove('opacity-0', 'scale-95');
                    contentElement.classList.add('opacity-100', 'scale-100');
                }, 10);
            }

            function closeModal(modalElement, contentElement) {
                contentElement.classList.remove('opacity-100', 'scale-100');
                contentElement.classList.add('opacity-0', 'scale-95');
                setTimeout(() => {
                    modalElement.classList.add('hidden');
                }, 300);
            }

            // --- Обработчики событий для меню донатов ---

            // Открытие модального окна донатов с информацией о выбранном уровне
            document.querySelectorAll('.open-donation-modal-btn').forEach(button => {
                button.addEventListener('click', (event) => {
                    const tier = event.target.dataset.tier;
                    const selectedTier = donationTiers[tier];

                    if (selectedTier) {
                        modalDonationTierName.textContent = selectedTier.name;
                        modalDonationTierDescription.textContent = selectedTier.description;
                        modalDonationPrice.textContent = selectedTier.price;
                        payDonationBtn.dataset.tier = tier; // Сохраняем выбранный уровень для кнопки оплаты
                        openModal(donationMenu, donationMenuContent);
                        vipDescriptionDiv.textContent = ''; // Очищаем описание VIP
                        donationImpactDescriptionDiv.textContent = ''; // Очищаем описание влияния
                        confirmationMessage.classList.add('hidden'); // Скрываем сообщение о подтверждении
                    }
                });
            });

            closeDonationMenuBtn.addEventListener('click', () => {
                closeModal(donationMenu, donationMenuContent);
            });

            payDonationBtn.addEventListener('click', () => {
                const selectedTier = payDonationBtn.dataset.tier;
                const price = donationTiers[selectedTier].price;
                vipDescriptionDiv.textContent = `Для получения доната "${donationTiers[selectedTier].name}" стоимостью ${price} рублей, пожалуйста, напишите в Telegram по номеру +797814378*7. Если вы угадаете последнюю цифру, вы получите донат!`;
                vipLoader.classList.add('hidden');
                learnMoreVipBtn.disabled = false;
                impactLoader.classList.add('hidden');
                generateDonationImpactBtn.disabled = false;
                confirmationMessage.classList.remove('hidden');
                setTimeout(() => {
                    confirmationMessage.classList.add('hidden');
                }, 5000);
            });

            learnMoreVipBtn.addEventListener('click', async () => {
                vipDescriptionDiv.textContent = '';
                vipLoader.classList.remove('hidden');
                learnMoreVipBtn.disabled = true;

                let currentChatHistory = [];
                const prompt = "Напиши привлекательное и подробное описание преимуществ VIP-статуса в системе донатов. Описание должно быть мотивирующим и включать такие преимущества, как эксклюзивный контент, ранний доступ, специальные значки, приоритетная поддержка и уникальные возможности.";
                currentChatHistory.push({ role: "user", parts: [{ text: prompt }] });
                const payload = { contents: currentChatHistory };
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-05-20:generateContent?key=${apiKey}`;

                let retries = 0;
                const maxRetries = 5;
                let delay = 1000;

                while (retries < maxRetries) {
                    try {
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });

                        if (response.status === 429) {
                            retries++;
                            await new Promise(res => setTimeout(res, delay));
                            delay *= 2;
                            continue;
                        }

                        const result = await response.json();
                        const aiResponsePart = result?.candidates?.[0]?.content?.parts?.[0]?.text;

                        if (aiResponsePart) {
                            vipDescriptionDiv.textContent = aiResponsePart;
                        } else {
                            console.error('Unexpected API response structure for VIP description:', result);
                            vipDescriptionDiv.textContent = 'Не удалось сгенерировать описание. Пожалуйста, попробуйте еще раз.';
                        }
                        break;
                    } catch (error) {
                        console.error('Ошибка при вызове Gemini API для VIP описания:', error);
                        vipDescriptionDiv.textContent = 'Произошла ошибка при получении описания. Пожалуйста, попробуйте еще раз.';
                        retries++;
                        await new Promise(res => setTimeout(res, delay));
                        delay *= 2;
                    }
                }

                vipLoader.classList.add('hidden');
                learnMoreVipBtn.disabled = false;
            });

            generateDonationImpactBtn.addEventListener('click', async () => {
                donationImpactDescriptionDiv.textContent = '';
                impactLoader.classList.remove('hidden');
                generateDonationImpactBtn.disabled = true;

                const selectedTier = payDonationBtn.dataset.tier;
                const price = donationTiers[selectedTier].price;
                const tierName = donationTiers[selectedTier].name;

                let currentChatHistory = [];
                const prompt = `Напиши, на что пойдет донат в размере ${price} рублей (${tierName}) для нашего проекта. Опиши конкретные примеры использования средств и их влияние. Сделай текст мотивирующим и позитивным.`;
                currentChatHistory.push({ role: "user", parts: [{ text: prompt }] });
                const payload = { contents: currentChatHistory };
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-05-20:generateContent?key=${apiKey}`;

                let retries = 0;
                const maxRetries = 5;
                let delay = 1000;

                while (retries < maxRetries) {
                    try {
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });

                        if (response.status === 429) {
                            retries++;
                            await new Promise(res => setTimeout(res, delay));
                            delay *= 2;
                            continue;
                        }

                        const result = await response.json();
                        const aiResponsePart = result?.candidates?.[0]?.content?.parts?.[0]?.text;

                        if (aiResponsePart) {
                            donationImpactDescriptionDiv.textContent = aiResponsePart;
                        } else {
                            console.error('Unexpected API response structure for donation impact:', result);
                            donationImpactDescriptionDiv.textContent = 'Не удалось сгенерировать информацию о влиянии. Пожалуйста, попробуйте еще раз.';
                        }
                        break;
                    } catch (error) {
                        console.error('Ошибка при вызове Gemini API для влияния доната:', error);
                        donationImpactDescriptionDiv.textContent = 'Произошла ошибка при получении информации о влиянии. Пожалуйста, попробуйте еще раз.';
                        retries++;
                        await new Promise(res => setTimeout(res, delay));
                        delay *= 2;
                    }
                }

                impactLoader.classList.add('hidden');
                generateDonationImpactBtn.disabled = false;
            });

            // Закрытие меню донатов при клике вне его содержимого
            donationMenu.addEventListener('click', (event) => {
                if (event.target === donationMenu) {
                    closeDonationMenuBtn.click();
                }
            });

            // --- Функции для статуса сервера Minecraft (симуляция) ---
          
